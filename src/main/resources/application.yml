# Hibernate JPA
spring:
  datasource:
    url: jdbc:mysql://10.211.55.3/mydatabase
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true